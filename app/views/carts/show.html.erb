<h1>Mon panier</h1>

<% @cart.cart_items.each do |item| %>
  <div>
    <strong><%= item.product.name %></strong>
    <p>Quantité : <%= item.quantity %></p>
    <p>Prix : <%= number_to_currency(item.product.price_cents / 100.0) %></p>
  </div>
<% end %>