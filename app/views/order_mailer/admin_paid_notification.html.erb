<h1>Nouvelle commande payée</h1>

<p>
  La commande #<%= @order.id %> vient d’être payée.
</p>

<h2>Client</h2>

<p>
  <%= @order.first_name %> <%= @order.last_name %><br>
  <%= @order.email %>
</p>

<h2>Articles</h2>

<ul>
  <% @order.order_items.each do |item| %>
    <li>
      <%= item.product_name %> × <%= item.quantity %> —
      <%= number_to_currency((item.unit_price_cents * item.quantity) / 100.0, unit: "€") %>
    </li>
  <% end %>
</ul>

<p>
  Total :
  <strong><%= number_to_currency(@order.total_price_cents / 100.0, unit: "€") %></strong>
</p>

<h2>Livraison</h2>

<p>
  <%= @order.first_name %> <%= @order.last_name %><br>
  <%= @order.address_line %><br>
  <%= @order.postal_code %> <%= @order.city %><br>
  <%= @order.country %>
</p>

<p>
  <%= link_to "Voir la commande dans l’admin", admin_order_url(@order) %>